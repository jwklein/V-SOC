nodes:
  indexer:
    - name: "{{ wazuh_indexer_name }}"
      ip: "{{ wazuh_indexer_ip }}"
  server:
    - name: "{{ wazuh_manager_name }}"
      ip: "{{ wazuh_manager_ip }}"
  dashboard:
    - name: "{{ wazuh_dashboard_name }}"
      ip: "{{ wazuh_dashboard_ip }}"